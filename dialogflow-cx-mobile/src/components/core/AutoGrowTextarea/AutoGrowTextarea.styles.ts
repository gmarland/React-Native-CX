import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textarea: {
    flex: 1,
    resizeMode: 'none',
    fontSize: 16,
    marginBottom: 3,
    paddingLeft: 16,
    paddingRight: 5,
    paddingTop: 15,
    paddingBottom: 14,
    zIndex: 10,
  },
});
