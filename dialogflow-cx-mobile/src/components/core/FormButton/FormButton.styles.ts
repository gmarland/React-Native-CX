import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    padding: 10,
    borderRadius: 4,
    alignItems: 'center',
    justifyContent: 'center',
  },
  responseText: {
    fontSize: 15,
    fontWeight: 'bold',
  },
  link: {
    padding: 10,
    borderRadius: 5,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
