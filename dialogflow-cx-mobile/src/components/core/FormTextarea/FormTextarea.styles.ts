import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  formTextInput: {
    fontSize: 17,
    padding: 15,
    borderRadius: 4,
    borderColor: '#ccc',
    borderWidth: 1,
    minHeight: 100,
  },
  erroredInput: {
    borderColor: '#ff0000',
  },
});
