import {StyleSheet} from 'react-native';

export const formStyles = StyleSheet.create({
  formContainer: {
    display: 'flex',
    flexDirection: 'column',
    gap: 20,
    width: '100%',
    padding: '5%',
  },
  formNav: {
    padding: '5%',
    paddingTop: 10,
  },
});
